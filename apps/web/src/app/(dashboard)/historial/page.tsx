// ─── app/(dashboard)/historial/page.tsx ───────────────────────────────────────
import { HistorialPage } from '@/features/history/historial-page';

export const metadata = { title: 'Historial — ZonaZero' };

export default function HistorialRoute() {
  return <HistorialPage />;
}
