// ─── app/(dashboard)/routes/page.tsx ──────────────────────────────────────────
import { RoutesPage } from '@/features/routes/routes-page';

export const metadata = { title: 'Rutas — ZonaZero' };

export default function RoutesRoute() {
  return <RoutesPage />;
}
