// app/(dashboard)/map/page.tsx
// Mapa en vivo — el LiveMap se renderiza en AppShell y es visible
// cuando pathname === '/map'. Esta página renderiza null; el mapa
// ya está montado de forma persistente en AppShell.

export const metadata = { title: 'Mapa en vivo — ZonaZero' };

export default function MapPage() {
  return null;
}
